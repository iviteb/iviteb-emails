<!-- Delivery or Pickup -->
{{#eq ../deliveryChannel "pickup-in-point"}}
  ((i18n.shipping.from))
  {{else}}
    ((i18n.shipping.upTo))
{{/eq}}
{{replace (replace (formatDate ../shippingEstimateDate) "/" ".") "/" "."}}